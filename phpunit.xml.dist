<?xml version="1.0" encoding="UTF-8" ?>

<phpunit
    colors="true"
    cacheResult="false"
    bootstrap="tests/bootstrap.php"
    displayDetailsOnAllIssues="true"
>

    <!-- code coverage reporting -->
    <source>
        <include>
            <directory suffix=".php">src</directory>
        </include>
    </source>

    <testsuites>
        <testsuite name="TanoWAF WAF-Core Test Suite">
            <directory>./tests</directory>
        </testsuite>
    </testsuites>

    <!-- all other env vars are now managed via a .conf.json file, and loaded in bootstrap.php -->
    <php>
        <env name="SERVER_TYPE" value="nginx"/>
        <env name="WAF_TYPE" value=""/>
        <env name="VERBOSITY" value="1"/>
    </php>
</phpunit>
